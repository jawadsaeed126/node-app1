 version: '3'
services:
  app1: # Changed from 'app' to 'app1'
    build:
      context: .
      dockerfile: Dockerfile
    image: my-service-app1:latest # Changed image name for clarity
    ports:
      - "3000:3000" # This port mapping is fine if only running one app at a time on the host
     
